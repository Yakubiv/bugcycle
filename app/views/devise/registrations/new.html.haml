%h3._center._m-b-xl Sign up

= form_for resource, as: resource_name, url: user_registration_path, html: { class: 'inline-labels' }, data: { validate: true } do |f|
  .form-group
    = f.text_field :name, placeholder: 'Your name', class: 'form-control', tabindex: 1, required: true, autofocus: true
  .form-group
    = f.email_field :email, placeholder: 'Your Email Address', class: 'form-control', required: true
  .form-group
    = f.password_field :password, as: :password, placeholder: 'Create a Password ', class: 'form-control', required: true
  .form-group._m-b-lg
    = f.submit 'Sign up', class: 'btn btn-info btn-block'
  ._m-t-xl._center
    Already have an account? #{render "devise/shared/links"}
